# Successful Project Detector

This model is trained on messages from the JuiceboxDAO Discord server, and predicts whether the sender is likely to create a Juicebox project which receives over 1 ETH.
